# ROADMAP · 优先级待办

> 排序原则：先修已知量表缺陷（低成本、影响每个用户），再补产品闭环，最后是需商业决策的大件。商业验证类里程碑（付费试点、常模样本量）见内部商业方案，本文件只列**产品/工程**待办。

## P0 · 量表 v2 修订（低成本、明确、先做）

- [ ] **合并 B3 近重复题**：B3-2（管教时另一方是否拆台）与 B3-3（教育分歧如何处理）构面相近，合并为一题，释放一个题位。
- [ ] **补"不适用"出口**：B3-4（孩子撞见吵架）等题对"从未发生"的家庭无中性选项，会被迫选中档。增设"不适用"跳转，且不计入该维度分母。
- [ ] **救回候选原表题**（择优补进释放的题位）：原表第 23（犯错管教，场景化改写）、第 44（允许不同意见）、第 10/13（双方父母与节日安排合成）。
- [ ] 任何题库改动：同步更新 `app.test.js` 规范哈希，保留 `source` 标注，跑禁词扫描。

## P1 · 产品闭环补齐

- [ ] **效标题盲区提示落地 H5**：目前效标对照主要在 xlsx 版；H5 端可在科目报告加"自评满意但得分偏低 → 盲区提示"。
- [ ] **驾照卡分享优化**：当前是可截图的证书卡。评估加"生成分享长图"或引导截图的引导层（注意微信分享合规，不做"分享解锁报告"）。
- [ ] **陪练完成与复检回环**：7 天陪练完成后，解锁最弱维度的"近 7 日快检"（不与首检比总分），出对比。当前复检为静态说明，未做交互闭环。
- [ ] **科目一与家庭科目的数据一致性**：科目一仍是 7 点 PERMA、独立存储；评估结果页是否需要三科统一的"总驾照"视图。

## P2 · 上线与合规

- [ ] **迁国内可访问托管**：github.io 微信打不开，正式投放前必迁（腾讯云 COS/CDN 或小程序化）。评估 H5 vs 小程序（小程序在测评裂变场景合规空间不同）。
- [ ] **数据合规基线**（若引入任何上传/账号）：测评数据按敏感个人信息处理——单独同意、最小必要、加密、可删除、18+ 限制、不静默留存。上线前需合规过目。
- [ ] **常模切换**：累计 300+ 真实样本后，把理论临时分档（85/70/55）换成百分位常模。需要一个能收集匿名分布的后端（与上一条一并设计）。

## P3 · 需商业决策后再启动

- [ ] **企业版（B2B2C）交付形态**：员工家庭关怀福利包、企业微信分发、企业只看聚合报告、匿名机制。依赖内部商业方案的验证结果，未定则不开工。
- [ ] **变现漏斗**：当前纯免费 demo。任何付费/私域/课程接入前，先过合规红线（见内部方案的"范爱豆判例"警示）。

## 工程债 / 小项

- [ ] git 提交身份配置（当前本机默认账号 + 未配置警告）。
- [ ] 考虑给 `main` 加最小 CI（`node --test`）防止未测试代码上线——目前靠人工纪律。
- [ ] `IMPLEMENTATION_PLAN.md` 是三科目升级的历史实施记录，可归档或并入本 ROADMAP。

---

### 怎么认领任务
每条任务动手前读对应 PRD；计分/量表类改动遵循"先写失败测试再实现"；改完 `node --test` 全绿 + 本地走流程 + `git push`。
